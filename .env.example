# API server port (default: 3001)
PORT=3001

# Set to production when deploying
NODE_ENV=development

# Postgres connection string — only needed when DB features are wired up
# DATABASE_URL=postgres://user:password@localhost:5432/buffetts_buffet
